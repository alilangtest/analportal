package ${basePackage}.service;

import java.util.List;

import ${clazzFullName};

public interface I${clazzShortName}Service {
	
	/**
	 * 新增${modelDesc}
	 * @param ${param_model}
	 * @throws ${exceptionName}
	 */
	void add(${clazzShortName} ${param_model}) throws ${exceptionName};
	/**
	 * 修改${modelDesc}
	 * @param ${param_model}
	 * @throws ${exceptionName}
	 */
	void modify(${clazzShortName} ${param_model}) throws ${exceptionName};
	/**
	 * 根据主键字符串删除多个${modelDesc}
	 * @param ${param_keys}
	 * @throws ${exceptionName}
	 */
	void deleteByKeys(String ${param_keys}) throws ${exceptionName};
	/**
	 * 根据主键查找${modelDesc}
	 * @return
	 * @param ${param_key}
	 * @throws ${exceptionName}
	 */
	${clazzShortName} queryByKey(String ${param_key}) throws ${exceptionName};
	/**
	 * 查询所有的${modelDesc}
	 * @return
	 * @throws Exception
	 */
	List<${clazzShortName}> queryAll() throws ${exceptionName};
}